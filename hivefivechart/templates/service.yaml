apiVersion: v1
kind: Service
metadata:
  name: {{ include "hivefivechart.fullname" . }}
  labels:
    {{- include "hivefivechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hivefivechart.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hivefivechart.fullname" . }}-test-connection"
  labels:
    {{- include "hivefivechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hivefivechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
